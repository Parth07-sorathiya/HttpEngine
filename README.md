# HttpEngine
HttpCore is a lightweight HTTP/1.1 server implemented from scratch in C++. It uses Linux sockets, epoll-based event handling, multithreading, request parsing, routing, connection management, logging, and HTTP response handling to demonstrate core web-server and network programming concepts.
